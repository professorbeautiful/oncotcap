package oncotcap.display.browser;

public class AMethod extends DependencyNoun {
				// event method name or a regular process method name
				public AMethod(PreprocessedCodeBundle cb, String varName) {
						super(cb, varName);
				}
				public AMethod(String className, String varName) {
						super(className, varName);
				}
		}
